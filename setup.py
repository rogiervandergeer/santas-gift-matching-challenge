from setuptools import setup, find_packages

setup(
    name = 'sgmc',
    version = '1.0.0',
    url = 'https://github.com/rogiervandergeer/santas-gift-matching-challenge',
    packages = find_packages() 
)